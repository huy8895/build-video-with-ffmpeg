name: 🌐 Translate SRT from Google Drive (Final Version)

on:
  workflow_dispatch:
    inputs:
      env:
        description: 'Chọn môi trường (ose, mep, dde)'
        required: true
        default: 'ose'
        type: choice
        options:
          - ose
          - mep
          - cdp
      folder_url:
        description: "Google Drive folder URL chứa file SRT gốc"
        required: true
      source_srt_filename:
        description: "Tên file SRT gốc trong folder (ví dụ: raw_cn.srt)"
        required: true
        default: "raw_cn.srt"
      languages:
        description: "Danh sách ngôn ngữ đích, cách nhau bằng dấu phẩy (ví dụ: English, Vietnamese, Japanese)"
        required: true
        default: "English, Vietnamese, Japanese"
      model:
        description: 'Gemini model (ví dụ gemini-2.5-pro)'
        required: false
        default: 'gemini-2.5-pro'
      thinking_budget:
        description: 'Thinking budget (-1 dynamic, 0 off, or positive int)'
        required: false
        default: '-1'

jobs:
  translate-and-upload:
    runs-on: ubuntu-latest
    # Khai báo output của job này, sẽ được job 'notify' sử dụng
    outputs:
      summary_message: ${{ steps.generate_summary.outputs.summary_message }}
    environment:
      name: ${{ inputs.env }}
    env:
      FOLDER_URL:       ${{ inputs.folder_url }}
      GG_REFRESH_TOKEN: ${{ secrets.GG_REFRESH_TOKEN }}
      YT_CLIENT_ID:     ${{ secrets.YT_CLIENT_ID }}
      YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
      GEMINI_API_KEY:   ${{ secrets.GEMINI_API_KEY }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-genai google-auth google-api-python-client

      - name: 📂 Download source SRT from Drive
        id: download_srt
        run: |
          set -euo pipefail
          SOURCE_FILE="${{ inputs.source_srt_filename }}"
          python scripts/download_drive_folder_files.py "$SOURCE_FILE"
          
          if [ ! -f "input/$SOURCE_FILE" ]; then
            echo "::error::Failed to download $SOURCE_FILE from Google Drive."
            exit 1
          fi
          
          echo "SRT_PATH=input/$SOURCE_FILE" >> $GITHUB_ENV
          echo "SRT_BASE_NAME=$(basename "$SOURCE_FILE" .srt)" >> $GITHUB_ENV
          echo "Downloaded file:"
          ls -lh "input/$SOURCE_FILE"

      - name: 🔄 Translate for each language
        id: run_translations
        continue-on-error: true # Cho phép job tiếp tục ngay cả khi step này thất bại
        run: |
          set -uo pipefail
          SUCCESSFUL_FILES=""
          SUCCESSFUL_LANGS=()
          FAILED_LANGS=()

          IFS=',' read -r -a LANG_ARRAY <<< "${{ inputs.languages }}"

          for lang_raw in "${LANG_ARRAY[@]}"; do
            lang=$(echo "$lang_raw" | xargs)
            echo "---"
            echo "🚀 Starting translation to: '$lang'"
            
            SAFE_LANG=$(echo "$lang" | tr ' ' '_')
            OUT_PATH="output/${{ env.SRT_BASE_NAME }}.${SAFE_LANG}.srt"
            mkdir -p output

            if ! python scripts/translate_srt.py \
              --input "${{ env.SRT_PATH }}" \
              --language "$lang" \
              --output "$OUT_PATH" \
              --model "${{ inputs.model }}" \
              --thinking-budget "${{ inputs.thinking_budget }}"; then
              
              echo "::warning::Translation to '$lang' failed. Skipping this language."
              FAILED_LANGS+=("$lang")
            else
              echo "✅ Translation to '$lang' succeeded. Output: $OUT_PATH"
              SUCCESSFUL_LANGS+=("$lang")
              SUCCESSFUL_FILES="$SUCCESSFUL_FILES $OUT_PATH"
            fi
          done

          echo "ALL_TRANSLATED_FILES=${SUCCESSFUL_FILES# }" >> $GITHUB_ENV
          
          # Xuất kết quả thô ra output của step để step sau sử dụng
          echo "successful_langs=${SUCCESSFUL_LANGS[*]}" >> $GITHUB_OUTPUT
          echo "failed_langs=${FAILED_LANGS[*]}" >> $GITHUB_OUTPUT
          echo "total_langs=${#LANG_ARRAY[@]}" >> $GITHUB_OUTPUT

          # Thoát với mã lỗi nếu có thất bại để đánh dấu step này là 'failed'
          if [ ${#FAILED_LANGS[@]} -ne 0 ]; then
            exit 1
          fi

      - name: 📝 Generate summary message
        id: generate_summary
        if: always() # Luôn chạy step này, bất kể step trước thành công hay thất bại
        run: |
          # Nhận output từ step trước
          SUCCESSFUL_LANGS="${{ steps.run_translations.outputs.successful_langs }}"
          FAILED_LANGS="${{ steps.run_translations.outputs.failed_langs }}"
          TOTAL_LANGS="${{ steps.run_translations.outputs.total_langs }}"
          
          # Chuyển chuỗi thành mảng để đếm số phần tử
          read -r -a successful_array <<< "$SUCCESSFUL_LANGS"
          read -r -a failed_array <<< "$FAILED_LANGS"
          success_count=${#successful_array[@]}
          failed_count=${#failed_array[@]}
          
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          SUMMARY_MESSAGE=""

          # Tạo thông báo động
          if [ $failed_count -eq 0 ]; then
            SUMMARY_MESSAGE="✅ **Dịch Thuật Hoàn Tất** ✅\n\nTất cả $success_count ngôn ngữ đã được dịch thành công!"
          elif [ $success_count -eq 0 ]; then
            SUMMARY_MESSAGE="❌ **Dịch Thuật Thất Bại** ❌\n\nToàn bộ $failed_count ngôn ngữ đều dịch thất bại. Vui lòng kiểm tra log."
          else
            SUMMARY_MESSAGE="⚠️ **Dịch Thuật Hoàn Tất (Có Lỗi)** ⚠️\n\n- *Thành công ($success_count/$TOTAL_LANGS)*: $SUCCESSFUL_LANGS.\n- *Thất bại ($failed_count/$TOTAL_LANGS)*: $FAILED_LANGS."
          fi
          
          SUMMARY_MESSAGE+="\n\n*Xem chi tiết tại*: [Link]($RUN_URL)"
          
          # Xuất thông báo hoàn chỉnh ra output của step này
          echo "summary_message<<EOF" >> $GITHUB_OUTPUT
          echo -e "$SUMMARY_MESSAGE" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: ☁️ Upload all results to Drive
        run: |
          if [ -z "${{ env.ALL_TRANSLATED_FILES }}" ]; then
            echo "No files were successfully translated. Skipping upload."
          else
            echo "Uploading successfully translated files: ${{ env.ALL_TRANSLATED_FILES }}"
            export UPLOAD_FILES="${{ env.ALL_TRANSLATED_FILES }}"
            python scripts/upload_results_to_drive.py
          fi

      - name: 📜 Show a preview of generated files
        run: |
          echo "✅ Translation process completed."
          echo "Generated files:"
          ls -lhR output/
      - name: "📩 Notify"
        run: |
          export MESSAGE="${{ steps.generate_summary.outputs.summary_message }}"
          node scripts/send_telegram_message.js
